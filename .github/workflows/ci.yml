name: CI

on: [push, pull_request]

jobs:

  build-linux-x64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: |
          apt-get update && apt-get install -y build-essential
          make SERVER_CFLAGS='-Werror'
      - name: Upload redis-server binary
        uses: actions/upload-artifact@v4
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: valkey-server-linux-x64
          path: src/valkey-server

  build-linux-x86:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: |
          sudo apt-get update && sudo apt-get install libc6-dev-i386
          make SERVER_CFLAGS='-Werror' 32bit
      - name: Upload redis-server binary
        uses: actions/upload-artifact@v4
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: valkey-server-linux-x86
          path: src/valkey-server

  build-linux-arm64:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: |
          apt-get update && apt-get install -y build-essential
          make SERVER_CFLAGS='-Werror'
      - name: Upload redis-server binary
        uses: actions/upload-artifact@v4
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: valkey-server-linux-arm64
          path: src/valkey-server

  build-macos-x64:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: make SERVER_CFLAGS='-Werror'
      - name: Upload redis-server binary
        uses: actions/upload-artifact@v4
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: valkey-server-macos-x64
          path: src/valkey-server

  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: make
        run: make SERVER_CFLAGS='-Werror'
      - name: Upload redis-server binary
        uses: actions/upload-artifact@v4
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: valkey-server-macos-arm64
          path: src/valkey-server

  create-release:
    runs-on: ubuntu-latest
    needs: [build-linux-x64, build-linux-x86, build-linux-arm64, build-macos-x64, build-macos-arm64]
    if: startsWith(github.ref, 'refs/tags/v')  # Run only if a tag is present
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/valkey-server-linux-x64/valkey-server
          asset_name: valkey-server-linux-x64
          asset_content_type: application/octet-stream
      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/valkey-server-linux-x86/valkey-server
          asset_name: valkey-server-linux-x86
          asset_content_type: application/octet-stream
      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/valkey-server-linux-arm64/valkey-server
          asset_name: valkey-server-linux-arm64
          asset_content_type: application/octet-stream
      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/valkey-server-macos-x64/valkey-server
          asset_name: valkey-server-macos-x64
          asset_content_type: application/octet-stream
      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/valkey-server-macos-arm64/valkey-server
          asset_name: valkey-server-macos-arm64
          asset_content_type: application/octet-stream